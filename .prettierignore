node_modules
pnpm-lock.yaml
.next
dist
build
coverage
.vercel

# Prose/docs are formatted by hand (ADRs, CLAUDE.md, plans) — leave them alone.
*.md

# Generated Drizzle migrations + snapshots
packages/*/drizzle

# Committed design reference (verbatim Claude Design export + screenshots) — never reformat
docs/reference
